{
  "name": "circleci-react",
  "version": "1.0.15",
  "private": true,
  "dependencies": {
    "@codeponder/common": "^1.0.14",
    "react": "^16.8.4",
    "react-dom": "^16.8.4",
    "react-scripts": "2.1.8"
  },
  "scripts": {
    "start": "cross-env SKIP_PREFLIGHT_CHECK=true react-scripts start",
    "build": "cross-env SKIP_PREFLIGHT_CHECK=true react-scripts build",
    "test": "cross-env SKIP_PREFLIGHT_CHECK=true react-scripts test --testResultsProcessor=jest-junit",
    "eject": "react-scripts eject",
    "ci:test": "npm run test",
    "ci:deploy": "npm run heroku:deploy",
    "heroku:deploy": "git push https://heroku:$HEROKU_API_KEY@git.heroku.com/insulution-web.git master"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "cross-env": "^5.2.0"
  }
}
